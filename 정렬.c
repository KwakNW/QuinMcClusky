
/*int selectsort4(int arr[][4], int num, int size) {
	int i, j, k;

	for (i = 0; i < size - 1; i++) {
		for (j = i + 1; j < size; j++) {
			if (arr[num][i] > arr[num][j]) {
				k = arr[num][i];
				arr[num][i] = arr[num][j];
				arr[num][j] = k;
			}
		}

	}
}
int selectsort8(int arr[][8], int num, int size) {
	int i, j, k;

	for (i = 0; i < size - 1; i++) {
		for (j = i + 1; j < size; j++) {
			if (arr[num][i] > arr[num][j]) {
				k = arr[num][i];
				arr[num][i] = arr[num][j];
				arr[num][j] = k;
			}
		}

	}
}*/